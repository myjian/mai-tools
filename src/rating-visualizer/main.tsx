import React from 'react';
import ReactDOM from 'react-dom/client';

import {RatingVisualizer} from './RatingVisualizer';

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<RatingVisualizer />);
